appId: com.hosts-manager.app
productName: Hosts Manager
directories:
  buildResources: resources
  output: dist
files:
  - out/**/*
  # 트레이 아이콘 — 메인에서 app.asar/resources/ 로 참조 (out만 넣으면 DMG에서 경로 없음)
  - resources/tray-iconTemplate.png
  - resources/tray-iconTemplate@2x.png
mac:
  target:
    - dmg
    - zip
  category: public.app-category.utilities
  icon: resources/icon.png
